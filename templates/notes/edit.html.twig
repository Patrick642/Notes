<div class="card {{ bgColor ?? 'bg_info' }}">
    {{ form_start(formEditNote, {'attr': {'id': 'edit-note'}, 'action': path('app_notes_edit', {'id': noteId }) }) }}
    <div class="card-header">
        {{ form_widget(formEditNote.title) }}
    </div>
    <div class="card-body">
        {{ form_widget(formEditNote.text) }}
    </div>
    <div class="card-footer">
        <div class="row gap-3">
            <div class="col d-flex align-items-center">
                <div class="d-flex flex-row gap-1">
                    {% for child in formEditNote.color %}
                    <label class="note-color-change d-block position-relative">
                        {{ form_widget(child) }}
                        <span class="checkmark rounded-circle {{ child.vars.label }}"></span>
                    </label>
                    {% endfor %}
                </div>
            </div>
            <div class="col-12" style="width: max-content">
                <button type="button" class="btn" data-bs-dismiss="modal">Close</button>
                <button type="submit" class="btn btn-primary">Edit</button>
            </div>
        </div>
    </div>
    {{ form_end(formEditNote) }}
</div>